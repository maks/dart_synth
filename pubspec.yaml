name: dart_synth
description: A simple command-line application.
version: 1.0.0
# homepage: https://www.example.com
publish_to: none

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  libao:
    git: 
      url: https://github.com/maks/libao
      ref: null-safety


dev_dependencies:
  pedantic: ^1.10.0
